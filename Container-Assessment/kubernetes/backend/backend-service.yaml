apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: muchtodo
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
